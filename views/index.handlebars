


{{#each burger}}
<h1>ID: {{id}}</h1>
<p>Name: {{burger_name}}</p>
<p>Devoured: {{devoured}}</p>

<button type="submit" class="submit-button" id={{id}}>Devour!</button>
{{/each}}

<script>

    $(document).on("click", ".submit-button", function(event){

        let id = event.target.id;

        $.ajax("/" + id, 
        {
            type: 'PUT',
            data: {id: id}
        }).then((res) => {

        });
        

    });

    
</script>